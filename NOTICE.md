Economy Bounties is an independent addon project for the Economy mod.

Public API packages are intended to remain stable once the first compatibility baseline is declared. Internal implementation packages may change without compatibility guarantees until then.
